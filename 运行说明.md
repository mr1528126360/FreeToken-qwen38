# FreeToken + DeepSeek-V4-Flash 运行说明

本机部署：2 × RTX 5090 D（32GB），服务实际使用 GPU 1；模型在
`/home/mr1528126360/models/DeepSeek-V4-Flash`（149GB，MoE 专家权重 offload 到内存）。

> **重要：DeepSeek 和 Qwen3.8-Flash-Next 不能同时运行。**
> DeepSeek 常驻约 153GB 内存，Qwen 需要约 174GB，合计远超 247GB 物理内存，
> 同时启动会触发 OOM killer 把两个服务一起杀掉。切换模型时先关闭再启动。

## 启动

每次启动必须带上修补版 CUDA 路径（修复了系统 CUDA 13.1 与新 glibc 的
`rsqrt` 头文件冲突，JIT 编译 kernel 需要）：

```bash
export CUDA_HOME=~/cuda-13.1-patched
export PATH=$CUDA_HOME/bin:$PATH
cd ~/FreeToken
source .venv/bin/activate

ft serve --model ~/models/DeepSeek-V4-Flash --gpu 1
```

前台运行可直接看日志；后台常驻（`--host 0.0.0.0` 表示监听局域网，去掉则只监听本机 127.0.0.1）：

```bash
nohup ft serve --model ~/models/DeepSeek-V4-Flash --gpu 1 --host 0.0.0.0 \
  --num-tokens 262144 --moe-cache-size 640 > ~/freetoken-serve.log 2>&1 &
```

说明：`--num-tokens 262144` 把上下文容量固定为 256k tokens；
`--moe-cache-size 640` 是配套值——256k 上下文的 KV+窗口池要占 ~8.5 GiB，
MoE 专家缓存需从默认 1238 slots 降到 640（5.8% 专家驻留 GPU），
否则总显存超预算启动失败。

加载约 3~5 分钟（154GB 专家权重从磁盘读入内存）。就绪判断：

```bash
ft ctl health          # 显示 status=ok 即就绪
# 或
curl -s http://127.0.0.1:1919/health
```

服务地址：本机 `http://127.0.0.1:1919`，局域网 `http://<服务器IP>:1919`
（当前已用 `--host 0.0.0.0` 启动，局域网内其他机器可直接访问）。
兼容 OpenAI API（`/v1/chat/completions`、
`/v1/models`、`/v1/responses`）和 Anthropic API（`/v1/messages`）。

注意：服务**没有任何鉴权**，局域网内任何机器都能调用；不要把端口暴露到公网。

## 关闭

- 前台启动：直接 `Ctrl+C`。
- 后台启动：

```bash
pkill -f "venv/bin/ft serve"
```

确认显存释放：`nvidia-smi`。

## 双卡 TP=2（2026-09-03 本分支新增，已实测）

专家按卡二分（每卡只加载/计算一半专家），attention 整层复制（block-fp8 不可切）。
改动清单见 [改动文档-GLM与DSV4双卡TP.md](改动文档-GLM与DSV4双卡TP.md)。

```bash
source /home/user/.cache/freetoken-cuda13.env
export NCCL_P2P_DISABLE=1 NCCL_SHM_DISABLE=1 NCCL_NET=Socket   # 双卡跨 NUMA 必须，否则 allreduce 死锁
cd ~/models/FreeToken
nohup .venv/bin/ft serve --model ~/models/DeepSeek-V4-Flash \
  --tp-size 2 --gpu 0,1 --port 1919 --host 0.0.0.0 \
  --num-tokens 262144 --moe-cache-size 1200 \
  > ~/models/dsv4-tp2.log 2>&1 &
```

实测（每卡 640 槽）：单流 decode **12.7~14.6 tok/s**（单卡 9.9~11.7，+25~30%）；每卡显存 ~29 GiB。
贪婪输出与单卡存在文本分叉（语义等价），为 all_reduce 浮点加法顺序所致，属正常。

**缓存调优（实测）**：`--moe-cache-size 1200`（每卡）时显存 ~36.4GB/卡，decode 提升至 **16.5~18.8 tok/s**
（640 槽 → 1200 槽，+40%）；1200 槽 ≈ 16GB/卡，TP 下该值是**每卡** slot 数。
贪婪输出与单卡存在文本分叉（语义等价），为 all_reduce 浮点加法顺序所致，属正常。
注意 `--moe-cache-size` 在 TP 下是**每卡** slot 数。
贪婪输出与单卡存在文本分叉（语义等价），为 all_reduce 浮点加法顺序所致，属正常。
注意 `--moe-cache-size` 在 TP 下是**每卡** slot 数。

## 日常使用

```bash
cd ~/FreeToken && source .venv/bin/activate

ft ctl health            # 服务状态
ft ctl stats             # 吞吐量、延迟、VRAM、缓存占用
ft ctl cache             # 查看 MoE/KV/SWA 缓存池
ft ctl requests          # 最近的请求记录

ft shell                 # 终端聊天界面（/help 查看命令）
```

## 缓存调优（在线调整，无需重启）

当前默认：上下文 256k tokens，MoE 缓存 640 slots（8.0 GiB，5.8% 专家驻留 GPU）。

提速档（上下文缩回 64k，MoE 缓存拉满，解码最快）：

```bash
ft ctl cache --moe 1238 --kv 64k --wait 300
```

超长上下文档（~38 万 tokens，MoE 缓存降到最小，解码明显变慢）：

```bash
ft ctl cache --moe 256 --kv 384k --wait 300
```

回到 256k 默认：

```bash
ft ctl cache --moe 640 --kv 256k --wait 300
```

## 上下文长度与显存（实测结论）

- 模型标称 1M 上下文，`/v1/models` 也报告 `max_model_len=1048576`，但**实际上下文上限受显存里的 KV/SWA 缓存池约束**，不是模型标称值。
- 全自动默认（不加参数启动）：KV 池只有 64,128 tokens，上下文太短；因此启动命令里固定为 **256k tokens**（KV 1.7 GiB + 窗口池 6.8 GiB，MoE 缓存相应降到 640 slots）。
- 在线扩容实测：MoE 缓存降到最小（256 slots）后 KV 可扩到 **393,216 tokens（384k）**，此时 VRAM 占用 15.9/18 GiB。再高（448k）就超出预算被拒绝。
- 实测 8.2 万 tokens 的长提示词：prefill 约 **622 tok/s**，"大海捞针"问题回答正确。
- **1M 上下文在本机（单卡 32GB）不可行**：KV 池本身只需 ~7 GiB，但配套的窗口/压缩注意力池随上下文线性增长（每 128 tokens 一页，约 136KB/token），1M 需要 ~34 GiB，超出整卡显存。实测把 KV 池强扩到 1M 后，跑 100 万 tokens 的提示词会直接把引擎跑崩（进程重启）。**建议长上下文最高用到 384k 那档。**
- 长上下文档位的代价：MoE 缓存只剩 2.3% 专家驻留 GPU，解码会比默认配置慢（更多专家走 PCIe/CPU）。

## 常见问题

- **启动时 nvcc 编译报错 `rsqrt` exception specification**：忘了设置
  `CUDA_HOME=~/cuda-13.1-patched`。
- **Python 依赖问题**：务必用 `~/FreeToken/.venv` 里的环境，不要用 base conda。
- **端口被占**：`--port 1920` 换端口。
- **服务无响应但进程还在**（引擎子进程崩了）：按"关闭"步骤杀掉再重启，
  日志见 `~/freetoken-serve.log`。
