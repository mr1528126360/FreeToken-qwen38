# FreeToken + Qwen3.8-Flash-Next 定制版部署文档

适用版本：`feat/qwen4-exp-tp-vision` 分支（基于 FreeToken 0.1.2，commit 9b5ee71）。
相比官方版新增：**双卡张量并行（TP=2）**、**1M 上下文（YaRN 扩展）**、**图片理解**。

## 一、硬件要求

| 项目 | 单卡模式（256k） | 双卡模式（1M） |
|---|---|---|
| GPU | 1 × NVIDIA RTX 5090（32GB）或同级 | 2 张，最好同型号 |
| 内存 | ≥ 200GB（模型 126GB + PLE 表 47.7GB + 余量） | ≥ 230GB（专家两卡分片各 63GB + PLE 每卡 47.7GB） |
| 磁盘 | 模型 126GB + 系统 | 同左 |
| 系统 | Linux x86_64，NVIDIA 驱动 r580+（CUDA 13） | 同左 |

## 二、系统准备

### 1. CUDA 工具链（关键坑）

CUDA 13.1 的 `crt/math_functions.h` 与新版 glibc（Ubuntu 26.04 等）冲突，
JIT 编译 kernel 会报 `rsqrt` exception specification 错误。**两种解法选一**：

**解法 A（推荐，不碰系统文件）**：复制一份 CUDA 到用户目录并打补丁：

```bash
mkdir -p ~/cuda-13.1-patched/targets/x86_64-linux
cd ~/cuda-13.1-patched
cp -r /usr/local/cuda-13.1/bin .
cp -r /usr/local/cuda-13.1/targets/x86_64-linux/include targets/x86_64-linux/
ln -s /usr/local/cuda-13.1/targets/x86_64-linux/lib targets/x86_64-linux/lib
sed -i 's|__device_builtin__ double                 rsqrt(double x);|__device_builtin__ double                 rsqrt(double x) __THROW;|; s|__device_builtin__ float                  rsqrtf(float x);|__device_builtin__ float                  rsqrtf(float x) __THROW;|' targets/x86_64-linux/include/crt/math_functions.h
```

之后所有 ft 命令前都要：

```bash
export CUDA_HOME=~/cuda-13.1-patched
export PATH=$CUDA_HOME/bin:$PATH
```

**解法 B**：把系统 CUDA 升到 13.2+（官方已修复）。

### 2. Python 环境

```bash
# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh
export PATH="$HOME/.local/bin:$PATH"

# 获取代码（替换为你的仓库地址）
git clone <你的仓库地址> && cd FreeToken
git checkout feat/qwen4-exp-tp-vision

uv venv --python 3.12
source .venv/bin/activate
```

### 3. 安装依赖

国内环境注意两个坑：

- uv 会把 `sglang-kernel` 钉到 GitHub releases（国内连不上）——**用 pip 代替 uv 装**（pip 会走 PyPI，两个包内容相同）
- 国内建议用清华镜像

```bash
source .venv/bin/activate
python -m ensurepip    # uv 的 venv 默认不带 pip
python -m pip install -e ".[accel]" --index-url https://pypi.tuna.tsinghua.edu.cn/simple
# 海外环境可以直接：uv pip install -e ".[accel]"
```

验证：

```bash
ft --version    # freetoken version 0.1.2
```

### 4. 带宽校准（每块 GPU 一次，启用 hybrid MoE 后端）

```bash
ft bench bw --gpu 0
ft bench bw --gpu 1   # 双卡模式才需要
```

### 5. 模型

下载 RadixArk NVFP4 版（126GB）：

```bash
# HuggingFace
huggingface-cli download RadixArk/Qwen3.8-Flash-Next-NVFP4 --local-dir ~/models/Qwen3.8-Flash-Next-NVFP4
# 或 ModelScope（国内更快）
modelscope download --model RadixArk/Qwen3.8-Flash-Next-NVFP4 --local_dir ~/models/Qwen3.8-Flash-Next-NVFP4
```

## 三、启动

### 单卡模式（256k 上下文，官方上限内，质量最稳）

```bash
export CUDA_HOME=~/cuda-13.1-patched
export PATH=$CUDA_HOME/bin:$PATH
cd ~/FreeToken

nohup .venv/bin/ft serve --model ~/models/Qwen3.8-Flash-Next-NVFP4 \
  --gpu 0 --port 1919 --host 0.0.0.0 \
  --num-tokens 262144 --moe-cache-size 1024 > ~/freetoken-qwen-serve.log 2>&1 &
```

### 双卡模式（1M 上下文 + 图片理解）

```bash
export CUDA_HOME=~/cuda-13.1-patched
export PATH=$CUDA_HOME/bin:$PATH
export FREETOKEN_LOAD_VISION=1
cd ~/FreeToken

nohup .venv/bin/ft serve --model ~/models/Qwen3.8-Flash-Next-NVFP4 \
  --tp-size 2 --gpu 0,1 --port 1919 --host 0.0.0.0 \
  --num-tokens 1048576 --max-seq-len-override 1048576 \
  --rope-scaling '{"rope_type":"yarn","factor":4.0,"original_max_position_embeddings":262144}' \
  --moe-cache-size 1024 --max-prefill-length 16384 > ~/freetoken-qwen-serve.log 2>&1 &
```

参数要点：

- `--moe-cache-size 1024` **必须显式给**（否则 MoE 缓存自动吃满显存，KV 分配 OOM 启动失败；TP 下该值是每卡的 slot 数）
- `--rope-scaling` + `--max-seq-len-override`：1M 是 YaRN×4 的 off-label 扩展（官方只背书 256k；82 万 token 检索实测通过）
- `--max-prefill-length 16384`：带图请求必须单分块处理，这个值覆盖任意尺寸的图
- `FREETOKEN_LOAD_VISION=1`：开启图片理解（纯文本场景可以不加）
- 去掉 `--host 0.0.0.0` 则只监听本机；服务无鉴权，勿暴露公网

加载约 5~8 分钟。就绪判断：

```bash
.venv/bin/ft ctl health    # status=ok 即就绪
```

## 四、关闭服务

```bash
pkill -f "venv/bin/ft serve"
sleep 10
# 确认显存释放；若有残留 worker 再按 PID 清理
nvidia-smi
for p in $(nvidia-smi --query-compute-apps=pid --format=csv,noheader); do
  case "$(ps -o cmd= -p $p 2>/dev/null)" in /usr/*) ;; *) kill -9 $p 2>/dev/null;; esac
done
```

## 五、使用

- OpenAI 兼容：`POST /v1/chat/completions`；Anthropic：`/v1/messages`；模型列表：`/v1/models`
- 图片请求（OpenAI 多模态格式）：

```json
{"model": "Qwen3.8-Flash-Next-NVFP4", "messages": [{"role": "user", "content": [
  {"type": "image_url", "image_url": {"url": "data:image/png;base64,<BASE64>"}},
  {"type": "text", "text": "描述这张图"}
]}]}
```

- 客户端参数建议：context window 填 1048576（双卡）/262144（单卡）；
  max output tokens 填 8192~32768（模型默认开思考，思考计入输出额度）

## 六、实测性能参考（2 × RTX 5090 D）

| 指标 | 双卡 TP=2 | 单卡 |
|---|---|---|
| prefill | ~2240 tok/s | ~2100~3100 tok/s |
| decode | ~44 tok/s | ~41 tok/s |
| 1M needle 检索 | 通过（82.7 万 tokens，7.2 分钟） | 显存放不下 |
| 图片理解 | 正确识别形状/颜色/文字 | 同 |

## 七、常见问题

| 现象 | 原因与解法 |
|---|---|
| nvcc 报 `rsqrt` exception specification | 没设 `CUDA_HOME=~/cuda-13.1-patched`，见二.1 |
| 启动时 CUDA OOM（分配 6GB KV 失败） | 没加 `--moe-cache-size 1024` |
| uv 安装卡在 sglang-kernel 下载 | GitHub releases 不通，改用 pip + 镜像，见二.3 |
| 图片请求 400 `image prompt is too long` | 图+文本超 16384 tokens，缩小图片或加大 `--max-prefill-length` |
| 服务进程还在但无响应 | 引擎子进程崩了，`pkill` 后看日志重启 |
| 与其他大模型服务共存 | 本模型内存占用 125GB（单卡）/190GB（双卡），不能与 DeepSeek-V4-Flash（153GB）同机同时运行 |
